# 修改前阶段计时

## 已有可测阶段

| 阶段 | 结果 | 判断 |
| --- | ---: | --- |
| DNS/TCP/TLS/上传前准备 | 141-298ms（热连接组） | 非主因 |
| 等待首字节 | 19.7-22.9s | 包含主要服务端处理 |
| 高清队列等待 | 0ms | 非主因 |
| 服务端总处理 | 18.967-21.832s | 慢 |
| LaMa 调用 | 18.642-19.140s | 主瓶颈 |
| 调用次数 | 1 | 非重复调用问题 |
| ROI | 1422×935，原图 88.52% | 根因触发器 |
| 输出 | 1495×999，ROI 外 0 像素变化 | 质量红线保持 |

## 修改前缺失的观测字段

当前代码没有独立记录 `requestReceivedAt`、上传保存、图片/遮罩解码、mask 构建、连通区域、ROI 构建、IOPaint 连接、请求编码、HTTP、IOPaint 输出编码、结果缩放、合成、结果编码和响应写出耗时；健康接口也缺少明确 `modelWarm` 与进程运行时间。下一步只在高清链路补齐这些字段，再用相同样本复测。

根因分类：`F. ROI过大` 导致 `H. LaMa推理` 过慢。A/B/C/D/E/G/I/J/K/L 在当前证据中不是主要耗时来源，其中客户端预览加载将在微信开发者工具阶段单独记录。
